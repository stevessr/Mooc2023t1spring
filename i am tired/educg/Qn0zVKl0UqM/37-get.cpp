#include <iostream>
#include <vector>
#include <set>
#include <sstream>
using namespace std;
int main()
{
    vector<vector<int>> sequences; // 用于存储所有输入的数列
    string line, temp = "";

    // 读取所有行
    while (getline(cin, line))
    {
        cout <<R"(")"<< line<<R"(\n",)";
    }
    return 0;
}